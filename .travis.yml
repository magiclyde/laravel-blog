language: php

php:
  - 7.1.9
  - nightly

install:
  - composer install --prefer-dist --optimize-autoloader --quiet

before_script:
  - cp .env.example .env
  - php artisan key:generate

script:
  - vendor/bin/phpunit